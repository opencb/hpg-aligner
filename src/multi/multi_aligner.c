#include <multi/multi_aligner.h>

int hpg_multialigner_main(options_t *options) {
  
  printf("START Multi Aligner\n"); 
  
  char *CLI_mapper = strdup(options->bwt_dirname);
  char *file_input = strdup(options->in_filename);
  
}
